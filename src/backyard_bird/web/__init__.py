"""Local status dashboard (§22) — read-only Flask app over the SQLite
timeline. Binds to localhost by default (§23.3). Nothing in this
package writes to the database; it exists to display what the capture
and analysis services have already recorded.
"""
